- @all_categories.each do |category|
  =link_to category.name, category_path(category)
table#articles
  %tbody
    - @articles.each do |article|
      %tr
        %td= article.title
        %td= article.author

