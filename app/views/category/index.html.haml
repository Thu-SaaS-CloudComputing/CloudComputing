%h1 XueTang Project
%table#Categories
  %thead
    %tr
      %th= 'All categories'
  %tbody
    - @categories.each do |category|
      %tr
        %td= link_to "#{category.name}", category_path(category)
